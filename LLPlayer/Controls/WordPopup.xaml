<UserControl x:Class="LLPlayer.Controls.WordPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:LLPlayer.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             x:Name="Root"
             d:Foreground="White"
             d:Background="Black"
             d:DesignHeight="450" d:DesignWidth="800">

    <Border
        DataContext="{Binding ElementName=Root}"
        ContextMenu="{Binding PopupContextMenu}"
        BorderBrush="{DynamicResource MaterialDesignDivider}"
        BorderThickness="1"
        CornerRadius="24"
        Padding="14 16"
        PreviewKeyDown="Border_OnPreviewKeyDown">
        <Border.Style>
            <Style TargetType="Border">
                <!-- Over video (default) a fixed dark chip stays readable on any frame; in the themed
                     sidebar follow the active theme so it matches Light mode. M3: rose-tinted surf3. -->
                <Setter Property="Background" Value="#382A30" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSidebar}" Value="True">
                        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <!-- TODO: L: Support dictionary API in addition to translations-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--TODO: L: Audio-to-speech features -->
                <TextBlock
                    Grid.Column="0"
                    x:Name="SourceText"
                    AutomationProperties.Name="Looked-up word"
                    Margin="0 0 8 0"
                    TextWrapping="Wrap"
                    Cursor="Hand"
                    MouseLeftButtonUp="SourceText_OnMouseLeftButtonUp"
                    FontWeight="Bold"
                    Foreground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                    FontSize="16"
                    />

                <!-- Close Button -->
                <Button
                    x:Name="CloseButton"
                    Grid.Column="1"
                    Style="{StaticResource MaterialDesignToolButton}"
                    Width="24" Height="24"
                    Margin="0,0,0,0"
                    AutomationProperties.Name="Close"
                    ToolTip="Close"
                    Click="CloseButton_OnClick"
                    PreviewMouseLeftButtonUp="CloseButton_OnMouseLeftButtonUp">
                    <materialDesign:PackIcon
                        Kind="Close"
                        Foreground="{DynamicResource MaterialDesignBody}"
                        Width="16" Height="16"/>
                </Button>
            </Grid>

            <!--Spinner-->
            <!--TODO: L: Binding errors occur in MaterialDesign 5.3.1
            ref: https://github.com/MaterialDesignInXAML/MaterialDesignInXamlToolkit/issues/3814-->
            <ProgressBar
                Grid.Row="1"
                IsIndeterminate="True"
                Width="20"
                Height="20"
                Style="{StaticResource MaterialDesignCircularProgressBar}"
                Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConv}}" />

            <TextBox
                x:Name="TranslationText"
                AutomationProperties.Name="Translation"
                AutomationProperties.LiveSetting="Polite"
                IsReadOnly="True"
                TextWrapping="Wrap"
                HorizontalAlignment="Center"
                materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                BorderThickness="0"
                Grid.Row="1"
                FontSize="20">
                <TextBox.Style>
                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                        <!-- Matches the Border: white over video, theme body colour in the sidebar. -->
                        <Setter Property="Foreground" Value="White" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSidebar}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Grid>
    </Border>
</UserControl>
